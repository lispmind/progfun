package com.progfun.worksheet

//import reflect.runtime.universe._

object TestFor {

  def main(args: Array[String]) {
    val list = List("A", "B", "C")
    //reify(for (e <- list; x = e; y = e) { println(x + y)})
//    reify(for (e <- list; x = e; y = e) {
//      println(x + y)
//    })
  }
}
